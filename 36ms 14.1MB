if not strs:
    return ''
elif not strs[0] or len(set(strs))==1:
    return strs[0]
res = ''
j = 0
while j<len(strs[0]):
    res+=strs[0][j]
    for i in range(1,len(strs)):
        if strs[i][:j+1] == res:
            continue
        else:
            return res[:-1]
    j+=1
return res
